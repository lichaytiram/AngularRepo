1) type - inside project, npm install gsap

2) type - on ts file 

import { gsap } from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";

inside ngOnInit function type 

gsap.registerPlugin(ScrollTrigger);